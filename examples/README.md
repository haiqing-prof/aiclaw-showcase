# Reference Example

The example in this directory is intentionally generic. It illustrates a safe routing pattern without importing the private AI-Claw codebase or any real data.

The rule gate handles an operationally sensitive question first. Only after the boundary is clear does the assistant produce a concise explanation and a human-owned next step.

